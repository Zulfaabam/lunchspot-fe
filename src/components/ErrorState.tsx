export function ErrorState({ retry }: { retry: () => void }) { return <main className="grid min-h-screen place-items-center bg-slate-50 p-6"><div className="rounded-3xl bg-white p-10 text-center shadow-sm"><h1 className="text-xl font-semibold">Unable to connect to LunchSpot server.</h1><button onClick={retry} className="mt-5 rounded-xl bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white">Retry</button></div></main> }
