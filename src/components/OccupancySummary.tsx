export function OccupancySummary({ available, total }: { available: number; total: number }) { const percent = total ? available / total * 100 : 0; return <section><p className="section-label">Available tables</p><p className="text-2xl font-semibold tracking-tight text-slate-900">{available} <span className="text-base font-medium text-slate-400">/ {total}</span></p><div className="mt-3 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-emerald-500 transition-all duration-500" style={{ width: `${percent}%` }} /></div></section>; }
