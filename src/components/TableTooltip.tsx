import { X } from 'lucide-react'; import type { CanteenTable } from '../types/canteen'; import { statusInfo } from '../utils/status';
export function TableTooltip({ table, onClose }: { table: CanteenTable; onClose: () => void }) { const s = statusInfo[table.statusId as 0|1|2]; return <aside className="absolute right-5 top-5 z-10 w-64 rounded-2xl border border-slate-100 bg-white/95 p-5 shadow-xl backdrop-blur"><button onClick={onClose} className="absolute right-3 top-3 rounded-lg p-1 text-slate-400 hover:bg-slate-100" aria-label="Close table information"><X size={16}/></button><p className="pr-5 text-lg font-semibold">{table.name}</p><div className="mt-4 space-y-3 text-sm"><Info label="Status"><span className={`inline-flex items-center gap-1.5 font-medium ${s.hex === '#f43f5e' ? 'text-rose-600' : 'text-emerald-600'}`}><i className={`h-2 w-2 rounded-full ${s.color}`} />{s.label}</span></Info><Info label="Seats">{table.seatCount} seats</Info><Info label="Table ID"><code className="text-xs text-slate-500">{table.id}</code></Info></div></aside>; }
function Info({ label, children }: { label: string; children: React.ReactNode }) { return <div><p className="text-xs font-medium uppercase tracking-wider text-slate-400">{label}</p><div className="mt-1 text-slate-700">{children}</div></div> }
